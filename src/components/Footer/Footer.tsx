import styles from './Footer.module.scss';

const Footer = () => {
  return (
    <footer className={styles.footer}>
        <img src="/images/footer/Logo.webp" alt="Логотип" />
    </footer>
  );
};

export default Footer;
